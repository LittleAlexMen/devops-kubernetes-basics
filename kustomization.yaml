apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - web-deployment.yaml
  - web-service.yaml
  - redis-deployment.yaml
  - redis-service.yaml
labels:
  - includeSelectors: true
    pairs:
      app: flask-redis
